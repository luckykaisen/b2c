<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.atguigu.dao.CartMapper">
	
	<select id="select_cart" resultType="com.atguigu.bean.T_MALL_SHOPPINGCAR">
		select * from t_mall_shoppingcar where yh_id = #{yh_id}
	</select>
	
	<insert id="add_cart" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
		insert into t_mall_shoppingcar(
			sku_mch,
			sku_jg,
			tjshl,
			hj,
			yh_id,
			shp_id,
			sku_id,
			shp_tp,
			shfxz,
			kcdz
			)values(
			#{sku_mch},
			#{sku_jg},
			#{tjshl},
			#{hj},
			#{yh_id},
			#{shp_id},
			#{sku_id},
			#{shp_tp},
			#{shfxz},
			#{kcdz}
		)
	</insert>
	
	<!-- 更新购物车中的商品信息 -->
	<update id="update_cart">
		update t_mall_shoppingcar 
		<set>
			sku_mch = #{old_cart.sku_mch},
			sku_jg = #{old_cart.sku_jg},
			tjshl = #{old_cart.tjshl},
			hj = #{old_cart.hj},
			yh_id = #{old_cart.yh_id},
			shp_id = #{old_cart.shp_id},
			sku_id = #{old_cart.sku_id},
			shp_tp = #{old_cart.shp_tp},
			shfxz = #{old_cart.shfxz},
			kcdz = #{old_cart.kcdz},
		</set>
		where sku_id = #{sku_id}
	</update>
</mapper>
